# NativeAOT 4 KiB Proof Exit Regression

## 1. Objective

This pass classified the reported `0xC0000419` exit from the bounded,
no-collection, 4 KiB repeated-allocation proof without changing the proven
Workstation GC first-refill loop, Workstation GC allocation behavior,
collection behavior, heap size, or expected allocation count.

The locked identities were NativeAOT source commit
`9d5a6a9aa463d6d10b0b0ba6d5982cc82f363dc3`, ILCompiler/runtime pack `9.0.0`,
AMD64, and active PAL archive
`C617D95647A20862947B52A1301DF96FE9104E5A13F11BB0C016B8370DDE115F`.

## 2. First-refill Outcome A baseline

The first-subsequent-refill experiment remains technically valid. Its
authoritative baseline is
`out/dotnet/gc-first-refill/run-20260801-110102440/manifest.json`:

- `byte[256]`, source-derived object size `0x118` / 280 bytes;
- 15 total allocations, 13 fast-path allocations, two real-GC allocations;
- two allocation-context refills, including the first subsequent refill;
- zero collections, zero finalization scans, and zero managed finalizers;
- process teardown PASS;
- ordinary kernel SHA-256
  `D68791B66BF268425B6E646F3EA94CE7B3777B97D9CCED6682C10A9E1389066C`.

The final regression in this pass reproduced the same result in three fresh
QEMU processes. It used a clean rebuilt artifact with PE
`689B421DF50126121069A9779A4C1C369A8FDE8D05D794AB00F420D6E061A15C` and ELF
`8F298C3973AD9CFE6F27381BD85DA74C8C120ABBC227A779C43B95993DD734F5`.

## 3. Reason closure was blocked

The live 4 KiB rerun supplied for this pass was reported to exit through the
opaque value `0xC0000419` before managed diagnostics. That initial observation
is preserved in the existing first-refill report and was not rewritten.

The available preserved failing-run inputs did not contain a serial log,
process-return record, WER record, or runner transcript that contained the
value. Therefore the value could not be tied to a generating instruction or
source line. The current immutable artifact instead reproduced a normal pass
through every captured layer.

## 4. Immutable failing 4 KiB artifact and preserved inputs

The preserved artifact and launch inputs are under
`out/dotnet/gc-first-refill-closure/four-kib-baseline/`.

| Input | SHA-256 |
|---|---|
| Managed proof PE | `46BFE1192562DE8ABAC4A87D94BADA93E3115568AC5898E01DB2F7D584555DAB` |
| Converted ELF | `17E7CE9DB772B0117FA04F0ED9669CDC191C401011308E929D309B1CAB7A082B` |
| Runtime-pack manifest | `4D9DC025201B2DBD81D11877777CD66BB05E4AC03481E3363B47E09A39AB1A0D` |
| Runtime-pack object | `055CA4FDE943F744CB48A89E8C5345565EDFF7010ECC6D9F8AB68493463EE0BF` |
| Preserved Server snapshot | `E0618180AF273E5FB666E51972B9CBD666491E7B0244BC85F1DD5992EC7542B1` |
| Kernel and ESP snapshot | `D68791B66BF268425B6E646F3EA94CE7B3777B97D9CCED6682C10A9E1389066C` |

The current source-rebuilt Server used for the final captures is
`BF31421CC5EA4B564EA50103A492E2CA8C37522B6561A05E76569A5C50074E96`.

## 5. Exit-propagation chain

The current path is a hosted guideXOS Server path; it does not use a QEMU
guest parser or a kernel experiment selector for the 4 KiB proof.

| Layer | Current observed value | Signed | Unsigned/hex | Generated or propagated |
|---|---:|---:|---:|---|
| Managed `ManagedMain` return | `0` | `0` | `0x00000000` | Generated by the managed proof contract |
| Native ELF entry wrapper | `0` | `0` | `0x00000000` | Propagates managed return |
| `NativeElfExecutor::Execute` | `0` | `0` | `0x00000000` | Stores `entry(...)` result and preserves it through cleanup |
| Server `nativeapp.smoketest` result | `0` | `0` | `0x00000000` | Propagates executor result |
| Server process | `0` | `0` | `0x00000000` | Normal process exit |
| PowerShell runner `$LASTEXITCODE` | `0` | `0` | `0x00000000` | Propagates native runner exit |

The reported `0xC0000419` is not present in any current source literal,
managed marker, Server marker, preserved serial log, raw process capture, or
runner stdout/stderr. The first generating layer is consequently **not
identified**; the value exists only as the initial external observation.

## 6. First origin of `0xC0000419`

No current layer generated the value. Two raw Server processes and three fresh
runner processes with immutable proof PE/ELF identities all returned
`0x00000000`. The preserved raw Server records include managed success, guest
return zero, normal `Exited` cleanup, and Server process return zero.

Windows Error Reporting contains a separate access-violation event for an
earlier Server process (`0xC0000005`, faulting module `gdi32full.dll`), but it
does not contain `0xC0000419`; it is preserved as context in
`exit-propagation/application-events.json` and is not treated as the reported
value's origin.

The exact missing invariant is a complete failing execution record captured at
the instant the value is first produced: process ID, raw `Process.ExitCode`,
WER record, final serial/Server marker, and the PowerShell native-command
return. Without that record, changing a runtime or runner contract would be
speculative.

## 7. 4 KiB versus 64 KiB comparison

The comparison is recorded in
`out/dotnet/gc-first-refill-closure/artifact-comparison/4k-vs-64k.json`.
Both paths use the same managed source, project, AMD64 entry address,
NativeAOT source commit, runtime-pack version, lock file, converter, runtime
identity family, no-collection strategy, and no GC-backed allocation path.

The intentional differences are only the `GUIDEXOS_MANAGED_HEAP_BYTES`
constant, the specialized runtime object/image hashes, and the resulting
geometry:

| Proof | Heap | Object | Expected allocations | Remaining |
|---|---:|---:|---:|---:|
| Small4KiB | 4096 | 280 | 14 | 176 |
| Primary64KiB | 65536 | 280 | 234 | 16 |

There is no QEMU/kernel selector, `EXTRA_CFLAGS` specialization, or alternate
controlled-OOM contract in the hosted 4 KiB/64 KiB execution path.

## 8. Controlled-OOM result contract

The no-collection allocator calls `guideXosManagedAllocationCanFit` before
`new byte[256]`. When the next object does not fit, it records controlled OOM
and returns `0` to managed code. It does not call `RhpNewArray`, return a null
object, throw, request collection, expand the heap, or invoke fail-fast.

The managed proof reports the successful result and returns `0`. The observed
4 KiB report is:

```text
Managed allocations completed: 14; heap=4096; object=280; remaining=176;
pointerBeforeFailure=...; pointerAfterFailure=...; monotonicity=PASS;
nonOverlap=PASS; sampledIntegrity=PASS; zeroInit=PASS;
collectionEntered=0; heapExpansionOccurred=0; controlledOom=1
```

## 9. Server return propagation

`NativeElfExecutionResult::exitCode` is an `int32_t`. The executor assigns it
from the Win64 entry call, uses it for the cleanup state decision, records it
in `NativeAppProcessTable`, and reports it as `returnCode`, `exitCode`, and
`gxMainReturnCode`. The Server command path returns the executor result; it
does not reinterpret the managed `0` as an NTSTATUS or sign-extend a managed
status into a process exception.

The raw process capture under
`out/dotnet/gc-first-refill-closure/exit-propagation/raw-process/` confirms
both fresh Server processes returned signed `0`, unsigned `0`, and
`0x00000000`.

## 10. Process-teardown behavior

For each successful 4 KiB launch, the last managed/host marker precedes:

```text
Native ELF gx_main returned 0
lifecycle state after execution: Exited
cleanup attempted: true
state=Exited exitCode=0
```

The no-collection proof reaches controlled OOM, returns normally, and then
enters ordinary NativeAOT artifact cleanup. It does not use the Workstation GC
shutdown boundary. Runtime-level GC shutdown remains unsupported for the
first-refill QEMU experiment; process teardown is the supported boundary.

## 11. Runner behavior

The repeated-allocation wrapper passes the child runner's `$LASTEXITCODE`
through a nonzero check. The hostlog runner separately checks preflight,
positive Server-session exit, exact managed markers, and the intentionally
nonzero invalid-input failure probe. A failure-probe nonzero result is not
allowed to overwrite the positive proof result.

Three final fresh runner captures used the same runner hash
`4E5CEC37209A533F888FBFC4F0B93C20130F75009B0E63AF9E73526A2E1B1D10` and
returned `0x00000000`. Their saved stdout, stderr, and PowerShell transcript
hashes are in `corrected/final-4k/analysis.json`.

## 12. Historical successful evidence

The historical bounded proof remains the documented 14-allocation result with
heap 4096, object 280, remaining 176, controlled OOM, zero collections, zero
GC-backed allocations, and zero expansion. The current immutable artifact
matches that contract and identity. The initially observed `0xC0000419` is
retained as a separate unclassified live observation; it is not substituted
into or used to rewrite the historical PASS.

## 13. Root cause

No source-backed root cause for `0xC0000419` was established. The evidence
supports a precise classification: **the reported exit is a non-reproduced,
unbound execution result, not a demonstrated no-collection runtime regression
and not a demonstrated runner translation**. The available evidence is
insufficient to distinguish a transient host/launch event from a stale or
mismatched external invocation.

## 14. Correction

No source correction was applied. In particular, the successful first-refill
managed loop, Workstation GC allocator, no-collection capacity, expected count
14, collection behavior, runtime identity checks, and PE/ELF layout were not
changed. The only repository correction was adding the new local evidence root
`/out/dotnet/gc-first-refill-closure/` to `.gitignore`.

## 15. Three corrected 4 KiB runs

The three final fresh runs use immutable PE
`46BFE1192562DE8ABAC4A87D94BADA93E3115568AC5898E01DB2F7D584555DAB`, ELF
`17E7CE9DB772B0117FA04F0ED9669CDC191C401011308E929D309B1CAB7A082B`, runtime
manifest `4D9DC025201B2DBD81D11877777CD66BB05E4AC03481E3363B47E09A39AB1A0D`,
and Server `BF31421CC5EA4B564EA50103A492E2CA8C37522B6561A05E76569A5C50074E96`.

| Run | Managed entries | Successful allocations | Controlled OOM | Collections | GC-backed | Expansion | Guest | Runner | Teardown |
|---:|---:|---:|---:|---:|---:|---:|---|---|---|
| 1 | 1 | 14 | PASS | 0 | 0 | 0 | PASS | PASS | PASS |
| 2 | 1 | 14 | PASS | 0 | 0 | 0 | PASS | PASS | PASS |
| 3 | 1 | 14 | PASS | 0 | 0 | 0 | PASS | PASS | PASS |

The fixed no-collection record has no separate `managedEntryCount` field; the
single `RhpReversePInvoke` entry is the managed-entry boundary. The exact
pre-exit state is therefore `managedEntryCount=1`, `allocationAttempts=15`
(14 fit checks plus the failing preflight check), `successfulAllocations=14`,
`expectedAllocations=14`, `controlledOomObserved=1`,
`lastAllocationReturnedNull=0/not applicable` (no failing `newarr` is issued),
`collections=0`, `gcBackedAllocations=0`, `heapExpansion=0`,
`managedReturnValue=0`, and `nativeWrapperReturnValue=0`.

## 16. 64 KiB regression

The fresh 64 KiB static and execution regression passed with PE
`942F5EF0DD441791A3D89B5DD9534731154F9F6D27B906971BB89285471D5B85`, ELF
`CCC3CD34B71E98D3F03C1B6B99DA9A1ADC335E6D46B0B97BC8AAB9917364FEAE`, 234
allocations, 16 bytes remaining, controlled OOM, zero collections, zero
GC-backed allocations, zero expansion, and normal teardown.

## 17. First-refill regression

The final clean first-refill run passed in three fresh QEMU processes:

- total allocations: 15;
- fast allocations: 13;
- real-GC allocations: 2;
- allocation-context refills: 2;
- object size: `0x118` / 280 bytes;
- collections: 0;
- finalization scans: 0;
- managed finalizers: 0;
- process teardown: PASS.

The specialized kernel was used only inside that smoke and the ordinary
kernel was restored afterward. The restored kernel and both `kernel/build` and
`ESP/kernel.elf` hash to
`D68791B66BF268425B6E646F3EA94CE7B3777B97D9CCED6682C10A9E1389066C`.

## 18. Remaining limitations

- The first producer of the externally reported `0xC0000419` is not proven.
- The failing serial/process/WER/PowerShell record needed to bind that value
  to a layer is absent from the available inputs.
- Runtime-level GC shutdown remains unsupported; process teardown is the
  tested boundary.
- The 4 KiB proof remains a hosted Server path, not a QEMU guest/parser path.
- No allocation after the first subsequent refill was attempted.

## 19. Decision outcome

**Closure Outcome B — the exit blocker is exact but correction remains a
separate pass.** The first-refill result remains technically valid, but the
milestone is not fully closed because the reported `0xC0000419` cannot be
assigned to a generating layer from the preserved evidence. No runtime or
runner correction is authorized on the basis of an unbound value.

Multiple subsequent refills, heap-segment transitions, new page commitments,
collection, and allocation beyond the established first-refill boundary are
not authorized.

## 20. Exact next experiment

After review, repeat only the bounded 4 KiB proof with the already preserved
PE/ELF and a process-launch capture that records, in one immutable run:

1. the exact Server PID and `Process.ExitCode`;
2. the final Server/managed marker and complete stdout/stderr;
3. the PowerShell `$LASTEXITCODE` before and after cleanup;
4. a timestamp-correlated Application Error/WER record if the process exits
   abnormally; and
5. the staged PE/ELF/runtime/kernel/ESP hashes.

Do not start multiple refills or segment-transition testing until that single
4 KiB event is either reproduced with a first generating layer or conclusively
shown to be an external harness/staging event.

## 21. Final immutable exit-provenance closure - 2026-08-01

The historical `0xC0000419` observation is preserved exactly as reported. It
was initially opaque because the failing capture did not contain a complete
same-run PID, raw-process, guest-marker, cleanup, WER, staging, and
PowerShell return chain. No source-backed association was made to Windows,
WER, QEMU, guideXOS, NativeAOT, PowerShell, or cleanup.

The final capture used the already-authorized 4 KiB snapshot at
`out/dotnet/gc-first-refill-closure/four-kib-baseline`. After freezing, no PE
build, PE-to-ELF conversion, specialized-kernel build, or runtime shutdown was
performed. The immutable identities were:

| Item | SHA-256 / identity |
|---|---|
| Managed PE | `46BFE1192562DE8ABAC4A87D94BADA93E3115568AC5898E01DB2F7D584555DAB` |
| Converted ELF and staged ELF | `17E7CE9DB772B0117FA04F0ED9669CDC191C401011308E929D309B1CAB7A082B` |
| Runtime-pack identity | `guidexos-nativeaot-runtime-pack-amd64-hostlog-repeated-allocation-nocollection-v1` |
| Runtime-pack manifest | `4D9DC025201B2DBD81D11877777CD66BB05E4AC03481E3363B47E09A39AB1A0D` |
| Runtime-pack object | `055CA4FDE943F744CB48A89E8C5345565EDFF7010ECC6D9F8AB68493463EE0BF` |
| Kernel | `D68791B66BF268425B6E646F3EA94CE7B3777B97D9CCED6682C10A9E1389066C` |
| ESP tree manifest (4 files) | `1A3C951A184A6FD0FA1FA730265B4D861E3FC4FE5B15F82B98B7219CBB04B894` |
| Proof runner | `4E5CEC37209A533F888FBFC4F0B93C20130F75009B0E63AF9E73526A2E1B1D10` |
| Capture runner | `AFE90B4722D4633FFDA64B88D24EC51236A22D238191098BFDF506884ADAF1E8` |

The reusable capture runner is
`scripts/capture-nativeaot-4k-exit-provenance.ps1`. Each run received a new
run ID and fresh stdout, stderr, serial-status, stage, and ESP paths. It
captured the child PID, creation and exit timestamps, signed/unsigned/hex
`Process.ExitCode`, immediate numeric PowerShell exit field before cleanup,
pipeline state, exceptions, runner return, timeout, cleanup/finally state,
all guest markers, and before/after PE, ELF, staged ELF, Server, kernel, and
ESP hashes. The hosted authoritative Server path does not use QEMU or emit a
serial stream; each run therefore records serial as explicitly not applicable,
while the same run captures the nativeapp/Server guest markers and teardown.
The PowerShell exit field is copied from the raw child exit immediately after
the .NET process wait, because this PID-preserving launch API does not itself
mutate PowerShell's automatic variable.

Five fresh immutable runs completed:

| Run | PID | Raw child exit (signed / unsigned / hex) | `$LASTEXITCODE` | Guest chain | WER |
|---:|---:|---|---:|---|---|
| 1 | 21872 | `0 / 0 / 0x00000000` | 0 | 1 entry, 15 attempts, 14 success, OOM 1, managed/native/Server 0, PASS | 1 correlated `0xC0000005` |
| 2 | 11108 | `0 / 0 / 0x00000000` | 0 | same; PASS | none |
| 3 | 20536 | `0 / 0 / 0x00000000` | 0 | same; PASS | none |
| 4 | 9564 | `0 / 0 / 0x00000000` | 0 | same; PASS | none |
| 5 | 25832 | `0 / 0 / 0x00000000` | 0 | same; PASS | none |

All five runs had zero collections, zero GC-backed allocations, zero heap
expansion, process teardown PASS, runner return 0, cleanup PASS, no cleanup
overwrite, and matching pre/post hashes. The one correlated WER Application
Error event was PID 21872, the exact immutable `server-snapshot.exe` path,
inside the execution window, with exception code `0xC0000005`. It is recorded
as a correlated diagnostic event, not as the proof result, and it does not
correlate the historical `0xC0000419` value. Correlated WER events for
`0xC0000419`: 0.

Final capture record: `out/dotnet/gc-first-refill-closure/exit-provenance-final/capture-20260801-153009004-11d42f22/summary.json`.

The historical value did not recur. First producing layer: none. Source-backed
origin: none. Final classification:

> Historical nonreproducible exit observation. No current layer in the immutable proof path generated or propagated `0xC0000419`.

The focused regressions after the capture set passed: 4 KiB `14` successful
allocations with controlled OOM; 64 KiB `234` allocations with controlled OOM;
startup-only QEMU; first collector-backed allocation; first subsequent refill
(`15` total / `13` fast / `2` refill); HostLog; runtime-pack state/hash; generic
ELF; and inventory isolation. Collections, finalization scans, and managed
finalizers remained zero where applicable. The ordinary kernel was restored to
`D68791B66BF268425B6E646F3EA94CE7B3777B97D9CCED6682C10A9E1389066C`.

## 22. Final decision

**Provenance Outcome A - historical value is nonreproducible; first-refill
milestone closed.** The historical observation remains preserved but is not
associated with the current immutable proof path and is not a current release
blocker.

Multiple refills are authorized only as the next bounded experiment: continue
primitive-array allocations through multiple allocation-context refills until
the first new heap-segment commitment or segment transition, without allowing
collection. After review, the exact checkpoint is:

```text
dotnet: validate first Workstation GC context refill
```

That checkpoint was authorized, not created or run, during this pass. No
multiple-refill or segment-transition testing occurred here.
