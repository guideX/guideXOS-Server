Phase 1 — Lock core contracts
•	IPC: finalize message schemas (Create/Close/Move/Resize/SetTitle/Invalidate/Frame/DrawRect/DrawText/Input events). Version them in gui_protocol.h.
•	Process/Task model: add exit codes, join/wait, timers, and backpressure on IPC queues.
•	FS: add a minimal in-memory VFS compatible with C# expectations for Desktop (dirs, attributes, file reads).
Phase 2 — Bring up a real GUI surface
•	Pick a renderer: Win32+GDI(+) or SDL2 for portability.
•	Update Compositor to own a native OS window, a backbuffer, and a render loop. Map bus messages to drawing and window lifecycle.
•	Add input (mouse/keyboard) capturing and publish to gui.output as typed events.
Phase 3 — Window and widget layer parity
•	Port Window/WindowManager concepts:
•	Window surfaces with z-order, focus, move/resize, invalidation.
•	Message routing (window events -> widget tree -> app).
•	Start a minimal widget toolkit: Label, Button, List, Dropdown (match C# classes and IDs). Emit OnClick/OnChange messages over IPC.
Phase 4 — GXM script support (fast path to visible UI)
•	Port GXMLoader.cs to C++ (gxm_loader.{h,cpp}). Parse simple “GUI script” and emit compositor messages.
•	Add CLI: gxm.load <file>, gxm.sample to spawn the C#-equivalent GUISamples windows.
Phase 5 — Desktop UX parity
•	Implement Desktop service: wallpaper, icons, pinned/recent, launchers. Keep state in a small JSON file.
•	Implement Taskbar/TaskView minimal features: list windows, activate/minimize/close.
Phase 6 — Default apps
•	Notepad (multi-line text, open/save via VFS).
•	File Explorer (list, open, context menu actions).
•	Console window bound to console_service via bus.
Phase 7 — Testing and tooling
•	Add unit tests (gtest) for allocator, scheduler, IPC bus, FS.
•	Add CLI smoke commands: gui.rect, gui.move, gui.resize, gui.event.pop, gxm.load.
•	Add a diagnostics page in the Razor Pages project to send bus commands (via a small localhost HTTP bridge), show running windows and logs.
Concrete next tasks (good first PRs)
•	Expand gui_protocol.h with: Move, Resize, SetTitle, Invalidate, DrawRect, DrawImage, InputKey, InputMouse.
•	Compositor: create a native window and present a backbuffer; render text/rects; forward input to gui.output.
•	Add gxm_loader.{h,cpp} and CLI command gxm.load to display a scripted window (port a subset of GXMScriptWindow from C#).
•	Implement WindowManager skeleton in C++: keep a map of windows, z-order, focus, and route input.
Keep each phase shippable: wire CLI commands to demo each new capability so you can test without full app parity. If you want, I can scaffold gui_protocol.h additions and a minimal Win32-backed compositor next.