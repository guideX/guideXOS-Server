# Phase 27M fixture

Recursion-safe user-function programs used by the Phase 27M bare-metal smoke.
The compiler emits ordinary rel32 calls and enforces a bounded runtime call
depth on every source-defined call edge.
