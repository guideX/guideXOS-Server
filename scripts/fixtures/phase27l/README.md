# Phase 27L fixture

This project exercises direct, nonrecursive integer functions in one source
file.  The compiler runs the source, symbol resolution, call graph, AMD64
lowering, and NativeElf creation inside guideXOS.
