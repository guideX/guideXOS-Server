# Phase 27K `break` / `continue` proof sources

These bounded sources exercise unlabeled `break` and `continue` in `while`
loops, nested innermost-loop targeting, skipped tails, host-call counts,
diagnostics, capacity rejection, and compiler reset behavior.
