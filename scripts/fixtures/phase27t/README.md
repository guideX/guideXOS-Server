# Phase 27T fixture

This three-file project proves named int-only structs, deterministic field
layout, `.`/`->` access, struct-pointer signatures, and field execution that
returns 42 through the bootstrap compiler and Developer Studio path.
