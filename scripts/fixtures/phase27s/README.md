# Phase 27S fixture

This project proves provenance-preserving `int*` traversal through a linked
four-element global array. The bootstrap smoke owns compilation, incremental
cache checks, runtime failure/recovery, and artifact inspection.
