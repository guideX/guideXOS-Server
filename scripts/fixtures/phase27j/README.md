# Phase 27J `while` loop proof sources

These bounded sources exercise pre-test `while` loops, genuine backward AMD64
branches, condition re-evaluation, nested control flow, short-circuit loop
conditions, repeated host calls, loop-local initialization, return paths, and
source-driven runtime state changes. They are compiled by the bare-metal
bootstrap compiler smoke harness.
