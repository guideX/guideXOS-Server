cmake_minimum_required(VERSION 3.16)
project(phase27i LANGUAGES CXX)

# The Phase 27I smoke path uses the kernel bootstrap compiler directly.
# This file keeps the fixture compatible with Developer Studio project
# validation and documents the source entry point for hosted tooling.
