# Copy this file to `ca-bundle.pem.local` and replace the placeholder text
# with a real public-root PEM bundle when you intentionally want to exercise
# the opt-in bare-metal public HTTPS probe.
#
# This repository does not ship public roots. The local `.local` file stays
# ignored by git, and the probe remains disabled or skip-safe until you stage
# an explicit public bundle through the documented flow.

# Paste one or more real public-root certificates below this line.
